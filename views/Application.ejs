<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;700&display=swap"
      rel="stylesheet"
    />
    <title>Application Form</title>
  </head>
  <body>
    <form action="/applyjob" method="post" class="container">
      <div>
        <!-- <label for="name">Full Name:</label> -->
        <input
          class="input-field"
          type="text"
          id="name"
          name="name"
          placeholder="Full Name"
        />
      </div>

      <div>
        <!-- <label for="email">Email:</label> -->
        <input
          class="input-field"
          type="email"
          id="email"
          name="email"
          placeholder="Email ID"
        />
      </div>

      <div>
        <!-- <label for="address">Address:</label> -->
        <input
          class="input-field"
          type="text"
          id="address"
          name="address"
          placeholder="Address"
        />
      </div>

      <div>
        <!-- <label for="phone">Phone Number:</label> -->
        <input
          class="input-field"
          type="tel"
          id="phone"
          name="phone"
          placeholder="Phone Number"
        />
      </div>

      <div class="resume">
        <label for="resume">Upload Resume</label>
        <input type="file" id="resume" name="resume" />
      </div>

      <button type="Submit" class="apply-btn">Apply</button>
    </form>
  </body>

  <style>
    * {
      margin: 0;
      padding: 0;
      font-family: "Inter", sans-serif;
      box-sizing: border-box;
      color: #212529;
    }

    .container {
      /* border: #1864ab solid 1px; */
      background-color: white;
      width: 400px;
      margin: 100px auto;
      padding: 50px 0;
      border-radius: 12px;
      box-shadow: 5px 5px 12px #868e969d;
    }

    form {
      display: flex;
      flex-direction: column;
      gap: 20px;
      align-items: center;
      justify-content: center;
    }

    .input-field {
      padding: 0 20px;
      height: 40px;
      width: 300px;
      border-radius: 6px;
      border: #1864ab solid 0.5px;
      /* box-shadow: inset 5px 5px 6px #868e969d;
      background-color: #e7f5ff; */
    }

    .input-field:focus,
    #resume:focus {
      outline: #1864ab solid 1.5px;
    }

    .input-field::placeholder {
      font-weight: 500;
      color: #495057;
    }

    .resume {
      display: flex;
      flex-direction: column;
      gap: 5px;
    }

    label {
      color: #495057;
      font-size: 14px;
      font-weight: 500;
    }

    #resume {
      border: #1864ab solid 0.5px;
      padding: 5px;
      border-radius: 6px;
      display: flex;
    }
    #resume::file-selector-button {
      background-color: #ced4da;
      color: #212529;
      border: none;

      font-weight: 700;
      padding: 8px 12px;
      border-radius: 6px;
      cursor: pointer;
    }

    .apply-btn {
      margin-top: 20px;
      background-color: #228be6;
      padding: 10px 30px;
      border: none;
      cursor: pointer;
      color: #fff;
      font-weight: 500;
      font-size: 18px;
      border-radius: 6px;
      transition: all 0.3s;
    }

    .apply-btn:hover {
      box-shadow: 0px 0px 12px #74c0fc;
    }
  </style>
</html>
